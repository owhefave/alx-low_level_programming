#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: to return 0
 */

int mul(int a, int b);

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;

	return (multiply);
}
