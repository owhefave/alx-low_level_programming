#include <stdio.h>
/**
 * main - printing numbers
 * Return: to return zero
 */
int main(void)
{
	int numbers;

	for (numbers = 0; numbers < 10; numbers++)
	{
		printf("%d", numbers);
	}
	printf("\n");
	return (0);
}
