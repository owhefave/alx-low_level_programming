#include "main.h"
/**
 * main - To print that print its own name
 * @argc: to hold the count of command line arguement
 * @argv: to hold the variable itself
 * Return: return 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

