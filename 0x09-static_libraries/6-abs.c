#include "main.h"
/**
 * _abs - This function is to print absolute values
 * @x: an integer
 * Return: To always return zero
 */
int _abs(int x)
{
	int r;

	r = _abs(x);

	_putchar(r);
	return (0);
}
