#include <stdio.h>
/**
 * main - To output some string 
 * Return: return only zero for success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
